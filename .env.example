# Directus backend API URL (defaults to Cardino's main instance if not provided)
NEXT_PUBLIC_DIRECTUS_URL=https://directus-production-ec98.up.railway.app

# Optional static admin token for server-side Directus queries if public role access is restricted
DIRECTUS_STATIC_TOKEN=
