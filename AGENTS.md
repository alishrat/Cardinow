# راهنمای توسعه‌دهندگان و سیستم هوشمند (AGENTS.md)
## پلتفرم کارت ویزیت دیجیتال مگاکارت (MegaCard)

این مستند شامل ساختار جامع پروژه، معماری سیستم، منطق اشتراک‌ها و قوانین توسعه برای هوش مصنوعی و توسعه‌دهندگان است.

---

### ۱. معرفی پروژه (Project Overview)
پلتفرم **مگاکارت (MegaCard)** یک سامانه جامع ساخت و مدیریت کارت ویزیت هوشمند دیجیتال مبتنی بر **Next.js 14 (App Router)** و **Directus CMS** به عنوان دیتابیس آنلاین و سرور مدیریت محتوا است.

#### نقش‌های کاربری (User Roles):
1. **مشتری (Customer):** ساخت و ویرایش کارت‌های ویزیت، مدیریت لینک‌ها و شبکه‌های اجتماعی، خرید و تمدید اشتراک‌ها، کیف پول و مشاهده آمار کارت‌ها.
2. **نماینده / تنانت (Tenant Manager):** مدیریت سیستم کارت‌های اختصاصی زیرمجموعه، تنظیمات برندینگ اختصاصی و دکمه‌های سفارشی.
3. **مدیر ارشد (Admin):** مدیریت کل سیستم شامل کاربران، پلن‌های اشتراک، قالب‌ها، تنانت‌ها، کارت‌ها و تراکنش‌های مالی.

---

### ۲. معماری فنی و ساختار پروژه (System Architecture)

```
├── app/
│   ├── layout.tsx              # روت اصلی و فونت‌های وزیرمتن
│   ├── page.tsx                # لندینگ پیج اصلی (معرفی، پلن‌ها، دموی قالب‌ها)
│   ├── dashboard/
│   │   └── page.tsx            # شل اصلی پنل مدیریت کاربران و نقش‌ها
│   ├── [slug]/
│   │   └── page.tsx            # صفحه عمومی نمایش کارت ویزیت همراه با ثبت بازدید هوشمند
│   └── api/
│       ├── cms/[...path]/      # روت اصلی پروکسی API با لایه امنیتی و توکن استاتیک
│       ├── directus/[...path]/ # روت سازگار پروکسی دایرکتوس
│       └── payment/
│           ├── request/        # درگاه پرداخت زرین‌پال (ایجاد شناسه پرداخت)
│           └── verify/         # تایید و اعمال خرید پلن یا شارژ کیف پول
├── components/
│   └── dashboard/              # کامپوننت‌های پنل مدیریت
│       ├── CustomerCardsView.tsx    # مدیریت و ویرایش پیشرفته کارت‌های مشتری
│       ├── CustomerBillingView.tsx  # پلن‌ها و خرید اشتراک مشتری
│       ├── CustomerWalletView.tsx   # کیف پول و تراکنش‌ها
│       ├── AdminCardsView.tsx       # مدیریت کارت‌ها توسط مدیر
│       ├── AdminPlansView.tsx       # تعریف و ویرایش پلن‌ها و سقف کارت‌ها
│       ├── AdminUsersView.tsx       # مدیریت کاربران
│       ├── AdminTenantsView.tsx     # مدیریت تنانت‌ها
│       └── AdminTransactionsView.tsx# گزارش تراکنش‌های مالی
├── lib/
│   ├── directus.ts             # سرویس دیتابیس، تبدیل توابع جلالی، فرمت اعداد و تایپ‌ها
│   ├── proxy-handler.ts        # هندلر هوشمند پروکسی API با مکانیزم بازنشانی توکن‌های منقضی
│   ├── styledQrCode.ts         # ساخت و دانلود کیوآر کد اختصاصی با لوگوی مرکز
│   └── vcard.ts                # خروجی استاندارد مخاطبین (.vcf)
└── .env.example                # متغیرهای محیطی مورد نیاز
```

---

### ۳. پروکسی هوشمند دیتابیس (Database API Proxy Logic)
تمام درخواست‌های فرانت‌اند به دیتابیس دایرکتوس از مسیر `/api/cms/...` عبور می‌کنند:
- **مدیریت توکن منقضی کاربر:** اگر مرورگر کاربر دارای توکن منقضی در `localStorage` باشد و درخواستی برای اطلاعات عمومی (مانند پلن‌ها، قالب‌ها یا کارت‌های عمومی) ارسال کند، در صورت دریافت خطای `401 Unauthorized` یا `403 Forbidden` پروکسی به طور خودکار درخواست را با **`DIRECTUS_STATIC_TOKEN`** تکرار می‌کند تا دریافت دیتای عمومی سایت هرگز دچار اختلال نشود.
- **حذف آلودگی هدرها:** پارامتر query string از نوع `access_token` پاکسازی شده و به صورت هدر استاندارد `Authorization: Bearer <token>` ارسال می‌گردد.

---

### ۴. منطق اشتراک‌ها و محدودیت‌ها (Subscription & Limits Engine)

#### الف) سقف مجاز کارت‌های ویزیت (`max_cards`):
- هر پلن دارای ویژگی `max_cards` است (`1` برای پلن پایه، `3` برای پلن حرفه‌ای، `-1` برای نامحدود).
- سیستم در زمان فشردن دکمه **«ساخت کارت ویزیت جدید»**، تعداد کارت‌های موجود کاربر را با `max_cards` پلن فعال وی مقایسه کرده و در صورت پر شدن سقف، کاربر را به صفحه ارتقای پلن هدایت می‌کند.

#### ب) تاریخ انقضای اشتراک (`subscription_end_date` / `end_date`):
- در صورت پایان یافتن مهلت اعتبار اشتراک کاربر:
  ۱. بنر هشدار منقضی شدن اشتراک همراه با تاریخ شمسی انقضا در بالای پنل مدیریت کارت‌ها به کاربر نمایش داده می‌شود.
  ۲. امکان ایجاد کارت جدید یا تغییر وضعیت کارت به فعال مسدود شده و پیام تمدید اشتراک ارائه می‌گردد.
  ۳. پس از پرداخت موفق در درگاه زرین‌پال (`/api/payment/verify`)، هم در جدول `subscriptions` و هم در فیلد `subscription_end_date` کاربر تاریخ جدید ثبت و اعمال می‌گردد.

---

### ۵. متغیرهای محیطی (Environment Variables)

کدهای پروژه به متغیرهای محیطی زیر متکی هستند:

```env
# آدرس اصلی دیتابیس دایرکتوس
NEXT_PUBLIC_DIRECTUS_URL=https://directus-production-ec98.up.railway.app

# توکن استاتیک ادمین برای استعلام‌های سروری در صورت محدودیت نقش عمومی
DIRECTUS_STATIC_TOKEN=

# مرچنت کد درگاه زرین‌پال
ZARINPAL_MERCHANT_ID=
```

---

### ۶. دستورات اصلی جهت تست و ساخت (Scripts & Verification)

- **کامپایل و بررسی تایپ‌ها:** `npm run build`
- **اجرای سرور توسعه:** `npm run dev`
